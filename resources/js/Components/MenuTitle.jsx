import React from 'react';

const MenuTitle = ({ title }) => {
    return (
        <div className="menut-title text-muted fs-5 fw-medium ms-4 mb-2 mt-2">
            {title}
        </div>
    );
};

export default MenuTitle;
